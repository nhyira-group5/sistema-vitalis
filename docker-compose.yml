services:
  nhyira-web:
    build:
      context: ./ 
    ports:
      - "80:80"  # Expõe a porta 80 do contêiner na porta 80 da máquina host
    volumes:
      - ./frontend:/home/ubuntu/frontend
    environment:
      - NODE_ENV=production
    restart: unless-stopped
